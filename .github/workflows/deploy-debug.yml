name: 🐛 Deploy Debug Environment

on:
  push:
    branches:
      - preview  # Technical branch name (kept for compatibility)
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual debug deployment'
        required: false
        default: 'Manual debug deployment triggered'
        type: string

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: debug
      url: ${{ steps.deployment.outputs.page_url }}debug/
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Leaflet library
        run: |
          ./download-libs.sh
          echo "✓ Leaflet library downloaded"

      - name: Prepare debug publish directory
        run: |
          set -euo pipefail
          rm -rf publish
          mkdir -p publish/debug
          
          if [ -d static ]; then
            # Copy everything from static into publish/debug
            cp -a static/. publish/debug/
            echo "✓ Static files copied to /debug/ path"
          fi
          
          # Generate fresh demo events with current timestamps
          if [ -f generate_demo_events.py ]; then
            python3 generate_demo_events.py > publish/debug/events.demo.json
            echo "✓ Generated fresh demo events with current timestamps"
          fi
          
          # Use debug config (debugging enabled, caching disabled, real events only)
          if [ -f config.debug.json ]; then
            cp config.debug.json publish/debug/config.json
            echo "✓ Using debug config (debug mode enabled, real events only)"
          elif [ -f config.preview.json ]; then
            cp config.preview.json publish/debug/config.json
            echo "✓ Using preview config (debug mode enabled, real events only)"
          elif [ -f config.dev.json ]; then
            cp config.dev.json publish/debug/config.json
            echo "✓ Using development config (fallback)"
          elif [ -f config.sample.json ]; then
            cp config.sample.json publish/debug/config.json
            echo "✓ Using sample config"
          fi
          
          # Inject build metadata (commit SHA, PR number) into config
          if [ -f publish/debug/config.json ]; then
            COMMIT_SHORT="${GITHUB_SHA:0:7}"
            
            # Try to get PR number from various sources
            PR_NUMBER=""
            if [ "${{ github.event_name }}" = "pull_request" ]; then
              PR_NUMBER="${{ github.event.pull_request.number }}"
            elif [ -n "${{ github.event.number }}" ]; then
              PR_NUMBER="${{ github.event.number }}"
            fi
            
            # Create build_info section using jq
            if command -v jq &> /dev/null; then
              BUILD_INFO=$(jq -n \
                --arg commit "$COMMIT_SHORT" \
                --arg sha "$GITHUB_SHA" \
                --arg pr "$PR_NUMBER" \
                --arg ref "$GITHUB_REF" \
                --arg actor "$GITHUB_ACTOR" \
                --arg timestamp "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
                '{
                  commit_short: $commit,
                  commit_sha: $sha,
                  pr_number: $pr,
                  ref: $ref,
                  deployed_by: $actor,
                  deployed_at: $timestamp
                }')
              
              # Merge build_info into config.json
              jq --argjson build_info "$BUILD_INFO" '. + {build_info: $build_info}' \
                publish/debug/config.json > publish/debug/config.json.tmp
              mv publish/debug/config.json.tmp publish/debug/config.json
              
              echo "✓ Injected build metadata: commit $COMMIT_SHORT, PR #$PR_NUMBER"
            else
              echo "⚠ jq not available, skipping build metadata injection"
            fi
          fi
          
          # DO NOT copy CNAME for debug environment (prevents custom domain conflict)
          rm -f publish/debug/CNAME
          
          # Prevent Jekyll processing
          touch publish/debug/.nojekyll
          
          # Dynamically determine base URL for debug environment
          # Support both GitHub Pages default URLs and custom domains
          if [ -f CNAME ]; then
            CUSTOM_DOMAIN=$(cat CNAME | tr -d '[:space:]')
            BASE_URL="https://${CUSTOM_DOMAIN}/debug/"
            echo "✓ Custom domain detected: ${CUSTOM_DOMAIN}"
            echo "  Debug environment will be available at: ${BASE_URL}"
          else
            # Use GitHub Pages default URL format
            REPO_NAME="${GITHUB_REPOSITORY#*/}"
            BASE_URL="/${REPO_NAME}/debug/"
            echo "✓ Using GitHub Pages default URL"
            echo "  Debug environment will be available at: https://${GITHUB_REPOSITORY_OWNER}.github.io${BASE_URL}"
          fi
          
          # Inject base tag for debug path if not present
          if [ -f publish/debug/index.html ]; then
            if ! grep -q '<base' publish/debug/index.html; then
              # Use relative path for base href (works with both custom domains and github.io)
              sed -i '/<head>/a\    <base href="/debug/">' publish/debug/index.html
              echo "✓ Base tag injected: <base href=\"/debug/\">"
              echo "  This works with both custom domains and GitHub Pages URLs"
            fi
          fi
          
          echo ""
          echo "Debug environment publish directory contents:"
          ls -la publish/debug
        env:
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: publish

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
